# M2 — Knowledge Check
> Module 2: Tools & Methodology

---

**Q1.** What does `%timeit` do in Jupyter? Write an example using it on a pandas operation.

**Q2.** What is the output of `pd.DataFrame??` vs `pd.DataFrame?` — what is the difference?

**Q3.** A colleague says *"I always skip the Business Understanding phase in CRISP-DM and go straight to data preparation — it saves time."* What problem is likely to occur, and why?

**Q4.** You run your notebook on a new machine and get a `ModuleNotFoundError: No module named 'statsmodels'`. What file should have prevented this problem, and how do you create it?
*(Lesson L2.4)*

**Q5.** What does `nbstripout` do, and why is it important for git commits in Jupyter projects?
*(Lesson L2.5)*

**Q6.** Explain the relationship between a Python `dict` and a pandas `DataFrame`. Give a code example that creates a DataFrame from a dict.

**Q7.** In CRISP-DM, you're in the Modeling phase and discover your model performs poorly on one group of customers. Which earlier phase should you revisit, and why?

**Q8.** True or False: *"conda and pip are interchangeable — it doesn't matter which you use."* Explain the key difference in one sentence.

**Q9.** Write the git commands to: (1) initialise a repo, (2) stage a notebook, (3) make a well-formed first commit.

**Q10.** After exporting your conda environment with `conda env export > environment.yml`, a teammate on a different OS reports they can't recreate it exactly. What flag should you add to the export command and why?
*(Hint: research `--no-builds`)*

---
*Answer key available from instructor after submission.*
